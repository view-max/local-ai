# Unrestricted Local AI

A lightweight, completely uncensored, offline terminal AI assistant designed to run efficiently on local hardware (including mobile setups and lower-spec devices).

## Installation
1. Clone this repository:
```bash
   git clone [https://github.com/view-max/unrestricted-local-ai.git](https://github.com/view-max/unrestricted-local-ai.git)
   cd unrestricted-local-ai
